# Changelog

## [Unreleased]

- Diff-only hunk and commit search
- Reusable on-disk embedding cache
- Path and extension filters
- Paginated commit graph
- Collapsible branch count, branch scope, and first-parent history filters
- Per-file diff grouping with max-file scoring for commit search
- Stable indexing progress animation
- Support all textual Git diffs, including manifests, lockfiles, documentation, and extensionless files
- Optional documentation-file exclusion in Target filters, enabled by default
- Single compact Settings panel for target filters, compare range, and query translation
- Keep secondary search and history controls inside Settings so the collapsed view focuses on the commit tree and results
- Show the selected branch and toggle states as muted text on the collapsed Settings row
- Highlight the selected commit range through graph rows, nodes, and edges, with stable colors for branch labels
